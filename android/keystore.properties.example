# Copy to android/keystore.properties (gitignored). Do not commit the real passwords.
# deploy/android-build.sh writes this on the server from /etc/chatter/keystore.properties.
storeFile=/path/to/chatter-release.jks
storePassword=change-me
keyAlias=chatter
keyPassword=change-me
