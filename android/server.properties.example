# Copy to android/server.properties (gitignored).
# Baked into a fresh install. Already-installed apps keep the address they saved.
# A build also accepts CHATTER_SERVER_URL, or https:// plus CHATTER_DOMAIN, and those win over this file.
serverUrl=https://chat.example.com
