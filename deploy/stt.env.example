# Live file: /etc/chatter/stt.env (not in git). deploy/stt-setup.sh writes it.
# The phone recognizes voice notes on device unless cloud recognition is turned on.
CHATTER_STT_URL=http://127.0.0.1:5090/v1/audio/transcriptions
CHATTER_STT_MODEL=sense-voice
CHATTER_STT_KEY=
