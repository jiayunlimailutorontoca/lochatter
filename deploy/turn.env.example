# Written for you by deploy/turn-setup.sh. Copy this only if you want to fill it by hand.
# The live file is /etc/chatter/turn.env and is not in git.
CHATTER_TURN_SECRET=replace-with-openssl-rand-hex-32
CHATTER_TURN_HOST=YOUR_SERVER_IP
